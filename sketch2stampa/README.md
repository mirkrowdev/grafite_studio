# Grafite — sketch2stampa

See the [root README](../README.md) for full documentation.

## Quick start

```bash
pip install -r requirements.txt
python3 app.py
```

On Ubuntu: `sudo apt install python3-tk` if Tkinter is missing.
